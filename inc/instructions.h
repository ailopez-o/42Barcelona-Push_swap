#ifndef INSTRUCCTIONS_H
# define INSTRUCCTIONS_H

# include "../inc/defines.h"

int sa(t_meta *meta);
int sb(t_meta *meta);
int ss(t_meta *meta);
int pa(t_meta *meta);
int pb(t_meta *meta);
int	ra(t_meta *meta);
int	rb(t_meta *meta);
int	rr(t_meta *meta);
int	rra(t_meta *meta);
int	rrb(t_meta *meta);
int	rrr(t_meta *meta);
#endif